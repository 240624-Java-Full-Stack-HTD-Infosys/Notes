::Angular Version::
What version of angular used JavaScript?
{
    =Angular 1
    ~Angular 2
    ~Angular 4
    ~None of the above
}

::Angular JS::
Which of the following statements is true?{
    =Angular JS is built on the MVC model
    ~Component-based architecture is implemented in Angular JS
    ~Angular JS is the latest version of Angular
    ~None of the Above
}

